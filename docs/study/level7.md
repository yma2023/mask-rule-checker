> ## 優先順位（これだけやれば読める順）
> 
> 1. **`std::string`** 
> 2. 参照 / const    
> 3. 例外処理 
> 4. `ifstream` / `ofstream` + `getline`  
> 5. `vector` + `size_t` + `auto&`
> 6. クラス / namespace（EasyMRC周り） 👈今回はここ！！
> 7. `argc` / `argv`
> 8. `chrono`
> 9. `thread`（おまけ）<br>
><br>
>
<br>
<br>

了解。
では **クラス / namespace（EasyMRC 周り）** を、
これまでと同じく **「HelloWorld → 気持ち悪さの正体を一つずつ潰す」** という流れで解説するね。
※ 指示どおり、`   ` は使わない。

   

# 第2弾-⑥ クラス / namespace（EasyMRC周り）

ここは C から来た人が
「突然オブジェクト指向とか言い出して意味不明」
となりやすい最大の山だけど、**実際は難しくない**。

   

## 第1章：まず namespace は「名前の住所」

### 1-1. C の世界で起きていた問題

C では、関数名や変数名は **全部グローバル**。

```c
int init();
```

別の人のコードにも `init()` があったら？
→ 名前衝突。

   

### 1-2. C++の解決策：namespace

```cpp
namespace easymrc {
    int init();
}
```

これは日本語にすると：

> 「この init は easymrc というグループに属する」

   

### 1-3. だからこう書く

```cpp
easymrc::init();
```

意味：

> 「easymrc に属する init を使う」

   

## 第2章：using namespace がやっていること

### 2-1. あなたのコードにあるこれ

```cpp
using namespace easymrc;
```

これは何かというと：

> 「これ以降、easymrc:: を省略していい」

   

### 2-2. 省略前と省略後

省略前：

```cpp
easymrc::EasyMRC checker(config);
```

省略後：

```cpp
EasyMRC checker(config);
```

👉 **中身は一切変わっていない**
👉 ただの記述量削減

   

## 第3章：クラスとは何か（超シンプル定義）

### 3-1. 一言定義

クラスとは：

> **「データ（変数）と処理（関数）をひとまとめにした型」**

   

### 3-2. Cでやっていたことを思い出す

```c
struct Config {
    int a;
    int b;
};

void run(struct Config* c){
    // 処理
}
```

👉
データと処理が **バラバラ**

   

## 第4章：C++のクラスは「それをまとめたもの」

### 4-1. 超最小クラス

```cpp
class Counter {
public:
    int value;

    void add(){
        value++;
    }
};
```

   

### 4-2. 使い方

```cpp
Counter c;
c.value = 0;
c.add();
```

ここで起きていること：

* `c` は Counter 型の変数
* `value` は c 専用の変数
* `add()` は c 専用の関数

👉 **struct + 関数 を一体化しただけ**

   

## 第5章：コンストラクタ（`()` の正体）

### 5-1. さっきの違和感を思い出して

```cpp
std::ifstream file("rules.txt");
EasyMRC checker(config);
```

「なんで () 付いてるの？」問題。

   

### 5-2. 正体：コンストラクタ

```cpp
class EasyMRC {
public:
    EasyMRC(Config cfg){
        // 初期設定
    }
};
```

意味：

> 「EasyMRC 型の変数を作るとき、
> Config を渡して初期化してね」

   

### 5-3. だからこうなる

```cpp
EasyMRC checker(config);
```

日本語訳：

> 「Config を使って初期化された EasyMRC 型の checker を作る」

   

## 第6章：メンバ関数呼び出し（`.` の意味）

### 6-1. これ

```cpp
auto results = checker.run(polygons);
```

分解すると：

* `checker`：EasyMRC 型の変数
* `run`：EasyMRC に属する関数
* `polygons`：引数

👉
**「checker 専用の run を呼んでいる」**

   

## 第7章：EasyMRC 全体を日本語に翻訳する

### 7-1. あなたのコードの核心部分

```cpp
EasyMRC checker(config);
auto results = checker.run(polygons);
```

完全日本語訳：

> 「設定 config を使って
> EasyMRC という処理装置 checker を作る。
> その装置に polygons を渡して検査を実行し、
> 結果を results として受け取る。」

   

## 第8章：Results や Config が何者か

### 8-1. Config

```cpp
EasyMRC::Config config;
```

意味：

> 「EasyMRC に付属している設定用の構造体」

Cで言うなら：

```c
struct Config config;
```

   

### 8-2. Results

```cpp
auto results = checker.run(polygons);
```

`results` の中身：

* `space_violations_type_a`
* `width_violations`
* `total_violations()`

👉 **結果をひとまとめにした箱**

   

## 第9章：namespace + class を一文で理解する

```cpp
easymrc::EasyMRC
```

これは：

> 「easymrc という名前空間に属する
> EasyMRC というクラス」

   

## 第10章：HelloWorldレベルのミニ例題

```cpp
#include <iostream>

namespace sample {

class Greeter {
public:
    Greeter(const std::string& msg){
        message = msg;
    }

    void say(){
        std::cout << message << "\n";
    }

private:
    std::string message;
};

}

int main(){
    sample::Greeter g("Hello Class");
    g.say();
}
```

やっていること：

* namespace でグループ化
* クラスでデータ＋処理をまとめる
* `()` は初期化
* `.` でメンバ関数呼び出し

   

## 今の到達点

ここまでであなたは：

* namespace = 名前の住所
* class = struct + 関数
* `()` = 初期化（コンストラクタ）
* `.` = 専用関数呼び出し

を **C感覚の延長**で理解できている。

👉 これで EasyMRC 周りは
「黒魔術」から「ちゃんとした構造」に見えるはず。

   

## 残っているピース

最後に自然につながるのは：

* argc / argv（CLIツール完成）
* chrono（時間計測）
* thread（おまけ）

次はどれ行く？
