
> ## 優先順位（これだけやれば読める順）
> 
> 1. **`std::string`** 👈今回はここ！！
> 2. 参照 / const
> 3. 例外処理
> 4. `ifstream` / `ofstream` + `getline`
> 5. `vector` + `size_t` + `auto&`
> 6. クラス / namespace（EasyMRC周り）
> 7. `argc` / `argv`
> 8. `chrono`
> 9. `thread`（おまけ）<br>
><br>
>
<br>
<br>

# スタート地点（そのまま）

```c
#include <stdio.h>

int main(void){
    printf("Hello World\n");
    return 0;
}
```

ここから **少しずつ C++ の要素を足していく**。

  

## ステップ1：C++でもまずは同じことができる

### ① ヘッダをC++用に変える

```cpp
#include <iostream>

int main(void){
    std::cout << "Hello World\n";
    return 0;
}
```

### 解説

* `printf` の代わりに `std::cout`
* `<<` は「右に流し込む」イメージ
* **Cとやってることは同じ（表示）**

👉 ここで
「C++は *書き方が違うだけ* で、流れは同じ」
と確認できればOK。

  

## ステップ2：文字列という「値」を変数に入れる

### ② 文字列を直接書かず、変数にする

```cpp
#include <iostream>

int main(void){
    const char* s = "Hello World";
    std::cout << s << "\n";
    return 0;
}
```

### 解説

* これは **Cのやり方**
* `char*` は「文字の並びを指している」

👉 ここまでは **完全にCの知識だけ**。

  

## ステップ3：C++の文字列 `std::string` を使う

### ③ C++版の文字列に置き換える

```cpp
#include <iostream>
#include <string>

int main(void){
    std::string s = "Hello World";
    std::cout << s << "\n";
    return 0;
}
```

### 解説（超重要）

* `std::string` は **「文字をまとめて扱う型」**
* 中身は意識しなくていい
* **char配列を安全に・簡単にしたもの**

👉 この時点では

> 「ああ、`char[]` の代わりの新しい型なんだな」
> で十分。

  

## ステップ4：文字列の長さを調べてみる

### ④ 「文字数」を表示

```cpp
#include <iostream>
#include <string>

int main(void){
    std::string s = "Hello World";
    std::cout << s << "\n";
    std::cout << s.size() << "\n";
    return 0;
}
```

### 解説

* `s.size()` は **string専用の関数**
* 「string型には便利な関数が最初から付いている」

👉 ここで初めて
**「型にくっついた関数（メンバ関数）」**
が登場。

  

## ステップ5：文字列の一部を切り出す（substr）

### ⑤ 先頭5文字だけ表示

```cpp
#include <iostream>
#include <string>

int main(void){
    std::string s = "Hello World";
    std::string sub = s.substr(0, 5);
    std::cout << sub << "\n";
    return 0;
}
```

### 解説

* `substr(開始位置, 長さ)`
* `0` は「先頭から」
* `5` は「5文字分」

👉
**配列の添字 + 長さ**
という感覚でOK。

  

## ステップ6：文字を探す（find）

### ⑥ `World` の開始位置を探す

```cpp
#include <iostream>
#include <string>

int main(void){
    std::string s = "Hello World";
    size_t pos = s.find("World");

    std::cout << pos << "\n";
    return 0;
}
```

### 解説

* `find()` は「どこにあるか」を探す
* 見つかれば **位置（番号）**
* 見つからなければ **特別な値**

👉
「文字列の中を検索できる」
＝ 設定ファイル解析の基礎。

  

## ステップ7：find + substr を組み合わせる

### ⑦ `key: value` を分解する最小例

```cpp
#include <iostream>
#include <string>

int main(void){
    std::string line = "rule_distance: 50.0";

    size_t pos = line.find(':');

    std::string key = line.substr(0, pos);
    std::string value = line.substr(pos + 1);

    std::cout << key << "\n";
    std::cout << value << "\n";

    return 0;
}
```

### 解説

* これが **あなたの本コードの核心**
* 設定ファイル1行を

  * 左（key）
  * 右（value）
    に分けている

👉 ここまで来たら
**`load_rule_file()` の前半は読める状態**。
